<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(Mr Cocktail.jpeg);">
  <div class="container">
    <div class="header"><h2>CAPANGA</h2></div>
    <a class="btn btn-flat" href="#"><h4><%= link_to 'Create a Cocktail', new_cocktail_path %></h4></a>
  </div>
</div>

  <% @cocktails.each do |cocktail| %>
   <li> <%= link_to cocktail.name.capitalize, cocktail_path(cocktail) %> </li>
  <% end %>
</ul>

